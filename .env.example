VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Set to "true" to surface the AI drill entry points. Leave unset until the
# `drill` edge function is deployed (supabase functions deploy drill), otherwise
# the links lead to a "coach unavailable" fallback.
VITE_ENABLE_DRILL=
